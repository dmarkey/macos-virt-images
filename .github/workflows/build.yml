name: Build
on:
  workflow_dispatch:
jobs:
  build-base:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: docker run -w $PWD -v $PWD ubuntu:22.04 sh -c "ls && pwd"
    #- uses: actions/upload-artifact@v2
    #  with:
    #    name: virt
    #    path: vmlinuz.tar.gz
